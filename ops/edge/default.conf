server {
  listen 80;
  server_name _;

  location = /healthz {
    default_type text/plain;
    return 200 "ok\n";
  }

  # Página inicial simples — elimina dependência do Redpanda Console
  location = / {
    default_type text/plain;
    return 200 "edge alive\n";
  }
}
